import{_ as t,o as i,c as a,ao as n}from"./chunks/framework.Ubw-3W2F.js";const u=JSON.parse('{"title":"EpiBranch.jl — Design","description":"","frontmatter":{},"headers":[],"relativePath":"design.md","filePath":"design.md","lastUpdated":null}'),s={name:"design.md"};function o(r,e,h,l,d,c){return i(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="EpiBranch.jl-—-Design" tabindex="-1">EpiBranch.jl — Design <a class="header-anchor" href="#EpiBranch.jl-—-Design" aria-label="Permalink to &quot;EpiBranch.jl — Design {#EpiBranch.jl-—-Design}&quot;">​</a></h1><p>This page is the high-level design: the ideas the package is built on and the reasons for them. It deliberately avoids type signatures, field layouts, and API names, which live with the code and in the <a href="/EpiBranch.jl/previews/PR168/tutorials/extending#Extending-EpiBranch">Extending guide</a> and <a href="./api">API reference</a>. The design principles below are the basis on which the shape here is judged.</p><h2 id="Design-principles" tabindex="-1">Design principles <a class="header-anchor" href="#Design-principles" aria-label="Permalink to &quot;Design principles {#Design-principles}&quot;">​</a></h2><p>These are the principles EpiBranch is meant to satisfy. They should be revisited when adding anything substantial, and the architecture should be reviewed against them periodically.</p><h3 id="1.-Simple-but-rigorous" tabindex="-1">1. Simple but rigorous <a class="header-anchor" href="#1.-Simple-but-rigorous" aria-label="Permalink to &quot;1. Simple but rigorous {#1.-Simple-but-rigorous}&quot;">​</a></h3><p>Express only what we need. New machinery earns its place only when an analysis we actually do can&#39;t be done without it. Prefer closed-form likelihoods over simulation when both are available and equivalent. One verb (<code>loglikelihood</code>, <code>simulate</code>) does the dispatch, without specialised wrapper functions per data type or model variant.</p><h3 id="2.-Self-explanatory" tabindex="-1">2. Self-explanatory <a class="header-anchor" href="#2.-Self-explanatory" aria-label="Permalink to &quot;2. Self-explanatory {#2.-Self-explanatory}&quot;">​</a></h3><p>Type names, function names, and signatures should match an epidemiologist&#39;s intuition for what they do. If a user has to read source to understand what a public name means, the name is wrong. The mathematical names (<code>Borel</code>, <code>GammaBorel</code>) are fair when they match the literature the user comes from; the operational names should match how an epidemiologist would describe what&#39;s happening.</p><h3 id="3.-Cleanly-separable-concerns" tabindex="-1">3. Cleanly separable concerns <a class="header-anchor" href="#3.-Cleanly-separable-concerns" aria-label="Permalink to &quot;3. Cleanly separable concerns {#3.-Cleanly-separable-concerns}&quot;">​</a></h3><p>Process model, observation model, data, inference, simulation, and output each own one thing. Their interfaces are explicit. A new alternative (a network model, multi-stream observation, time-varying reporting, aggregated counts) slots in by implementing the relevant interface rather than by editing core code. Each concern is replaceable independently.</p><h3 id="4.-Extensible-from-outside" tabindex="-1">4. Extensible from outside <a class="header-anchor" href="#4.-Extensible-from-outside" aria-label="Permalink to &quot;4. Extensible from outside {#4.-Extensible-from-outside}&quot;">​</a></h3><p>A user can add their own transmission model, observation model, intervention, or data type as a separate package or script, reusing all framework infrastructure. The contracts they have to satisfy are documented and small. Adding a custom piece does not require editing EpiBranch.</p><h3 id="5.-Documented-with-examples" tabindex="-1">5. Documented with examples <a class="header-anchor" href="#5.-Documented-with-examples" aria-label="Permalink to &quot;5. Documented with examples {#5.-Documented-with-examples}&quot;">​</a></h3><p>Principle 4 is empty without 5. Every public extension point has a worked example. Tutorials are checked at build time so the prose stays consistent with the code.</p><h2 id="Core-idea" tabindex="-1">Core idea <a class="header-anchor" href="#Core-idea" aria-label="Permalink to &quot;Core idea {#Core-idea}&quot;">​</a></h2><p>The offspring draw is completely decoupled from timing and from interventions. Who infects whom is a pure probabilistic object; when transmission happens, and whether a control measure stops it, are separate layers laid on top. This separation is what connects the package to survival analysis: the competing-risks framework on the transmission hazard is the same machinery as Kenah&#39;s pairwise survival analysis and dynamic survival analysis.</p><h2 id="The-model-is-the-generative-specification" tabindex="-1">The model is the generative specification <a class="header-anchor" href="#The-model-is-the-generative-specification" aria-label="Permalink to &quot;The model is the generative specification {#The-model-is-the-generative-specification}&quot;">​</a></h2><p>A <em>model</em> is the whole generative specification of how data arises: a transmission process, the population it acts on (attributes), the policy in force (interventions), and how cases are observed. The process carries the last three itself, so one object answers both &quot;what could this produce?&quot; (<code>simulate</code>) and &quot;how likely was this data?&quot; (<code>loglikelihood</code>).</p><p>Interventions, attributes, and observation belong to the model rather than to <code>simulate</code>, because a simulation-based likelihood has to reproduce the same generative process that produced the data. If isolation suppressed transmission in the observed outbreak, the likelihood of those chain sizes is only correct if it also applies isolation. With the three on the model, <code>loglikelihood(data, model)</code> already has them, and the two entry points can never disagree. A scenario sweep is then a map over models, each scenario a process built with a different policy, the usual Turing/SciML idiom. A counterfactual is a fresh model built with the policy you want; there is no in-place &quot;swap one input&quot; helper, so a model&#39;s pieces are always explicit at construction.</p><p>The three concerns stay distinct so a model can compose them without entangling them:</p><ul><li><p><strong>process</strong> — how the disease spreads and progresses;</p></li><li><p><strong>interventions</strong> — what is done about it (isolation, vaccination, tracing);</p></li><li><p><strong>attributes</strong> — who the people are (age, susceptibility).</p></li></ul><p>The process knows nothing about the policy, and the policy is the same object whichever process it acts on. Each of the three is optional: a process opts into the ones it uses and inherits the engine integration and the simulation-based likelihood without re-implementing them.</p><h2 id="Three-separated-stages" tabindex="-1">Three separated stages <a class="header-anchor" href="#Three-separated-stages" aria-label="Permalink to &quot;Three separated stages {#Three-separated-stages}&quot;">​</a></h2><p>The engine resolves every transmission in three stages. Only the first is model-specific; the other two run the same way for every model.</p><h3 id="1.-Offspring-draw-model-specific" tabindex="-1">1. Offspring draw (model-specific) <a class="header-anchor" href="#1.-Offspring-draw-model-specific" aria-label="Permalink to &quot;1. Offspring draw (model-specific) {#1.-Offspring-draw-model-specific}&quot;">​</a></h3><p>Pure branching process: no time, no interventions. For a parent, contacts are drawn from an offspring distribution: a single count, or a count per type for a multi-type model. The mean (R) and overdispersion (k) come from that distribution; if a contact matrix is supplied, it sets the mixing pattern across types.</p><p>This is the only stage a transmission model defines, and it does so in one of two ways. An <strong>offspring-driven</strong> model (the branching process and its variants) can produce its candidates one parent at a time, returning a count. A <strong>structure-driven</strong> model (a network, household, or metapopulation process) cannot, because a susceptible may be reachable by several infectious neighbours at once and infections deplete a fixed pool; it instead names the candidate contacts each infectious node reaches, and the engine resolves a node reached several times in one generation once. The companion <code>EpiNetwork.jl</code> package&#39;s network process is the worked example. Either way the model only says who contacts whom.</p><h3 id="2.-Timing-shared" tabindex="-1">2. Timing (shared) <a class="header-anchor" href="#2.-Timing-shared" aria-label="Permalink to &quot;2. Timing (shared) {#2.-Timing-shared}&quot;">​</a></h3><p>Each candidate is given a transmission time from the parent&#39;s infectiousness profile (the generation-time distribution). That distribution can be fixed or built per individual, so the timing can read any per-individual quantity: the parent&#39;s incubation period, or anything an attributes function has stored. This is the <em>potential</em> time of transmission: h(t) in survival-analysis terms.</p><h3 id="3.-Competing-risks-shared" tabindex="-1">3. Competing risks (shared) <a class="header-anchor" href="#3.-Competing-risks-shared" aria-label="Permalink to &quot;3. Competing risks (shared) {#3.-Competing-risks-shared}&quot;">​</a></h3><p>Each candidate is resolved independently to infected or not, by a single per-pair decision composed of a list of risk sources. Parent infectiousness and contact susceptibility are not privileged engine checks: they are default risk sources on the same surface an intervention uses, and isolation truncation or any risk an intervention contributes joins the same list. A model can contribute its own sources here too, for a transmission term that belongs to the <em>edge</em> rather than a node (a network&#39;s per-edge probability): the per-node susceptibility and infectiousness terms cannot carry a per-pair quantity without forcing one shared value across a node&#39;s edges, so it goes here instead. A contact is infected only if no risk blocks it, so the earliest removal before the contact&#39;s time wins, with no special min-logic.</p><p>Contacts that fail a check are still stored. They are contacts that were made but did not transmit, and they carry the contact-tracing table and intervention-effort tracking (contacts traced, vaccines administered, tests used) with no extra bookkeeping.</p><h3 id="Why-this-separation-matters" tabindex="-1">Why this separation matters <a class="header-anchor" href="#Why-this-separation-matters" aria-label="Permalink to &quot;Why this separation matters {#Why-this-separation-matters}&quot;">​</a></h3><p>Because the offspring distribution is a pure probabilistic object, it can be analysed with standard tools: extinction probability from the dominant eigenvalue, chain-size distributions, analytical likelihoods. None of these depends on timing or interventions, and the draw stays differentiable where the distribution permits. Interventions act on the timing and competing-risks layers, not the offspring layer: isolation truncates the hazard, contact tracing shifts the truncation earlier, vaccination lowers susceptibility. The generation-time CDF evaluated at an intervention time <em>is</em> the survival function, so the same objects appear in simulation and in Kenah&#39;s pairwise likelihood, and intervention effectiveness can be estimated from observed generation times using the quantities used to simulate.</p><h2 id="Extension-by-dispatch" tabindex="-1">Extension by dispatch <a class="header-anchor" href="#Extension-by-dispatch" aria-label="Permalink to &quot;Extension by dispatch {#Extension-by-dispatch}&quot;">​</a></h2><p>New behaviour is added by defining a new type and a method, not by growing options on an existing struct. A user wanting a variant should be able to write a small struct plus one or two methods, with no edits to the package source and no copy-pasting of existing function bodies. A <code>Union</code> field whose members trigger different branches, a <code>Symbol</code> that switches behaviour inside a function, or a <code>Bool</code> that selects a policy are all signals that a seam is in the wrong place and should become a dispatched-on type.</p><p>This holds on every axis:</p><ul><li><p><strong>Transmission models</strong> — spatial, network, and immunity dynamics enter through new model subtypes or composable wrappers, not flags on the branching process.</p></li><li><p><strong>Interventions</strong> are orchestrators of smaller dispatched pieces. An intervention struct is a thin shell wiring together independently dispatched components (eligibility, fire-rate, delay, effect), each a type with a method. The intervention body holds no hardcoded policy branching. Composition then works at two levels: between interventions (the stack the model carries) and within each intervention (its pieces).</p></li><li><p><strong>Output, observation, and outcome rules</strong> follow the same shape: mortality, hospitalisation, reporting, stopping conditions, and line-list columns are typed objects with methods, not closed sets of fields.</p></li></ul><p>The test of correctness for any component: can a plausible new variant be added without editing the component&#39;s source? If not, the component is doing too much, and the varying part should be lifted into a dispatched-on trait. The concrete contracts (which methods each axis requires, with worked examples) are in the <a href="/EpiBranch.jl/previews/PR168/tutorials/extending#Extending-EpiBranch">Extending guide</a>.</p><h2 id="Individual-state" tabindex="-1">Individual state <a class="header-anchor" href="#Individual-state" aria-label="Permalink to &quot;Individual state {#Individual-state}&quot;">​</a></h2><p>Each individual carries a small typed core that the engine reads, plus an open dictionary for everything else. The core holds its place in the transmission tree, its infection time, and the two universal modifiers (susceptibility and infectiousness). The dictionary is the deliberate extension hatch: interventions, attributes builders, clinical transitions, and observation models each own a small set of keys, and the engine never inspects them.</p><p>A typed core would couple the struct to every intervention&#39;s state shape and would break the dose-label namespacing that lets multi-dose vaccination schedules coexist. The dictionary keeps the individual independent of which pieces a user composes. The keys the package itself reserves, and the convention for naming keys added from other packages, are listed in the <a href="/EpiBranch.jl/previews/PR168/tutorials/extending#Extending-EpiBranch">Extending guide</a>.</p><h2 id="Interventions" tabindex="-1">Interventions <a class="header-anchor" href="#Interventions" aria-label="Permalink to &quot;Interventions {#Interventions}&quot;">​</a></h2><p>All interventions map onto two numbers: <strong>susceptibility</strong> (probability of infection given exposure, reduced by vaccination, prior immunity, or population depletion) and <strong>infectiousness</strong> (a modifier on onward transmission, reduced by isolation, treatment, or asymptomatic status). A contact is infected only if it survives the parent&#39;s infectiousness check, its own susceptibility check, and the timing check (generation time versus isolation time), the competing-risks resolution of stage 3.</p><p>Interventions are stacked and applied in order, each owning its own state and declaring the fields it requires. Time-dependent policies (start on day 14, stop after N cases) are expressed by wrapping an intervention in a schedule rather than by giving every intervention its own start-time field; the gate is on the <em>action</em> time, so an individual infected before a policy starts can still be affected if the time they would be tested falls after it. The hook contract and a worked example are in the <a href="/EpiBranch.jl/previews/PR168/tutorials/extending#Extending-EpiBranch">Extending guide</a>.</p><h2 id="Multi-type-branching-processes" tabindex="-1">Multi-type branching processes <a class="header-anchor" href="#Multi-type-branching-processes" aria-label="Permalink to &quot;Multi-type branching processes {#Multi-type-branching-processes}&quot;">​</a></h2><p>Multiple types (age groups, risk groups, spatial patches) are supported in the offspring draw: for a parent of type <code>j</code>, offspring counts per type are drawn from a joint distribution, with the mixing pattern from a contact matrix and the count family from the offspring distribution. Each contact is allocated to a type, and interventions and output are unchanged because they operate on individual-level state, not on types.</p><h2 id="Analytical-and-simulation-duality" tabindex="-1">Analytical and simulation duality <a class="header-anchor" href="#Analytical-and-simulation-duality" aria-label="Permalink to &quot;Analytical and simulation duality {#Analytical-and-simulation-duality}&quot;">​</a></h2><p>Where a closed form exists, EpiBranch uses it; simulation covers the rest, and the two are kept consistent.</p><p>The simulation side is extended through the intervention and model protocols above. The analytical side uses dispatch on the existing model types, with no new abstract type. Two kinds of extension plug in:</p><ul><li><p><strong>Offspring specifications</strong> replace what a branching process draws per individual, for example letting the offspring parameters vary from chain to chain. They participate in simulation through the offspring draw and in analytics by returning a chain-size distribution.</p></li><li><p><strong>Observation models</strong> capture how the latent process generates data. An observation returns a <em>transformed distribution</em> of the latent chain-size law, so it goes through the same likelihood path as the latent law and needs no bespoke likelihood method; each observation is written once, as a transform. On the simulation side it marks observed cases on a finished run.</p></li></ul><p>Some combinations have closed forms. Poisson offspring with a Gamma-distributed rate gives the <code>gborel</code> law from epichains, and these plug in by specialising the chain-size distribution on the relevant type combination, so dispatch picks the closed form without the user asking. The generic case falls back to quadrature.</p><p>Any extension with both an analytical chain-size distribution and a simulation path should have a regression test confirming they agree; the test suite provides a helper for this.</p><h3 id="Which-sampler-to-use" tabindex="-1">Which sampler to use <a class="header-anchor" href="#Which-sampler-to-use" aria-label="Permalink to &quot;Which sampler to use {#Which-sampler-to-use}&quot;">​</a></h3><p>Analytical likelihoods are deterministic scalar functions of the parameters: they work with any AD backend and with gradient-based samplers like NUTS. Simulation-based likelihoods draw random numbers, so the output is a noisy estimate and the gradient of a single realisation is not a useful estimate of the gradient of the expected likelihood, so gradient-based samplers should not be used. Use gradient-free samplers (Metropolis–Hastings, particle methods) instead, as the inference tutorial shows.</p><h3 id="Why-mutate-in-place" tabindex="-1">Why mutate in place <a class="header-anchor" href="#Why-mutate-in-place" aria-label="Permalink to &quot;Why mutate in place {#Why-mutate-in-place}&quot;">​</a></h3><p>The engine works in place, one generation at a time. Copying the whole state every generation would be far too expensive for an unbounded tree, so the engine mutates on purpose.</p><h2 id="Connection-to-survival-analysis" tabindex="-1">Connection to survival analysis <a class="header-anchor" href="#Connection-to-survival-analysis" aria-label="Permalink to &quot;Connection to survival analysis {#Connection-to-survival-analysis}&quot;">​</a></h2><p>The generation-time distribution g(t) = h(t)/R is the normalised infectiousness profile, and its CDF G(t) is the cumulative hazard. When isolation occurs at time t_iso, P(transmission before isolation) = G(t_iso) and P(transmission after) = 1 − G(t_iso): the transmission process is right-censored. The generation-time distribution connects to the population growth rate through the Euler–Lotka equation R = 1/M_g(−r). The same two objects, the generation-time distribution and the censoring time, appear in both simulation and Kenah&#39;s pairwise likelihood, so inference built on this framework fits the same quantities it simulates from.</p><h2 id="Host-timeline-and-transmission-route-windows" tabindex="-1">Host timeline and transmission-route windows <a class="header-anchor" href="#Host-timeline-and-transmission-route-windows" aria-label="Permalink to &quot;Host timeline and transmission-route windows {#Host-timeline-and-transmission-route-windows}&quot;">​</a></h2><p>The three stages above describe the simplest model: one offspring law, one generation-time distribution. The branching process generalises this by carrying a <strong>host timeline</strong> (the case&#39;s natural history as a sequence of timed states from infection: infectious, onset, severe, died or recovered, buried) and treating transmission as a set of <strong>route windows</strong> keyed off that timeline. A route window is an offspring law, a <code>from</code> state where infectiousness begins, the states that end it, and a survival kernel for the timing within it.</p><p>This is what lets several mechanisms become one. A funeral route runs between death and burial; a nosocomial route between admission and discharge; isolation lowering R is a route cut short by a removal state. The community route is the simplest window (from infection, no censoring), which recovers today&#39;s model unchanged. The latent period becomes the infection→infectious transition, the generation interval is derived from the latent period plus the kernel draw, and isolation truncation becomes one removal state among death, recovery, and burial.</p><p>The generalisation lands entirely in stages 2 and 3; stage 1 stays the pure, analysable layer. R remains the intrinsic reproduction number a case would make if never removed, and the realised R falls out of the censoring: shortening the infectious window blocks more contacts and lowers it. k remains the intrinsic offspring dispersion and is deliberately not the shape of the infectious period, so the count is never drawn from a duration, which would couple stage 1 to timing and break the branch-first decoupling the engine rests on.</p><p>A window contributes contacts only once its <code>from</code> state has occurred, so a survivor never materialises funeral contacts and nothing is created only to be censored. The offspring law driving outbreak size is therefore a fate-mixture (community offspring for everyone, plus funeral offspring for the fraction who die), which stays closed-form when each branch is a negative binomial and falls back to simulation otherwise, so the analytical/simulation duality carries over.</p><p>This is also the seam through which a household- or metapopulation-structured model in the wider ecosystem reuses the same survival objects at a smaller scale: a household is one route window scoped to a clique, with the contact-interval kernel as the window kernel and the infectious period as its censoring.</p><h2 id="References" tabindex="-1">References <a class="header-anchor" href="#References" aria-label="Permalink to &quot;References {#References}&quot;">​</a></h2><ul><li><p>Kenah E, Lipsitch M, Robins JM (2008). Generation interval contraction and epidemic data analysis. <em>Mathematical Biosciences</em> 213(1):71–79. <a href="https://doi.org/10.1016/j.mbs.2008.02.007" target="_blank" rel="noreferrer">doi:10.1016/j.mbs.2008.02.007</a></p></li><li><p>Kenah E (2011). Contact intervals, survival analysis of epidemic data, and estimation of R0. <em>Biostatistics</em> 12(3):548–566. <a href="https://doi.org/10.1093/biostatistics/kxq068" target="_blank" rel="noreferrer">doi:10.1093/biostatistics/kxq068</a></p></li><li><p>KhudaBukhsh WR, Choi B, Kenah E, Rempala GA (2020). Survival dynamical systems: individual-level survival analysis from population-level epidemic models. <em>Interface Focus</em> 10(1):20190048. <a href="https://doi.org/10.1098/rsfs.2019.0048" target="_blank" rel="noreferrer">doi:10.1098/rsfs.2019.0048</a></p></li><li><p>Wallinga J, Lipsitch M (2007). How generation intervals shape the relationship between growth rates and reproductive numbers. <em>Proceedings of the Royal Society B</em> 274(1609):599–604. <a href="https://doi.org/10.1098/rspb.2006.3754" target="_blank" rel="noreferrer">doi:10.1098/rspb.2006.3754</a></p></li></ul>',67)])])}const m=t(s,[["render",o]]);export{u as __pageData,m as default};
