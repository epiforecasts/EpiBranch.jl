import{_ as t,o as i,c as s,ao as a}from"./chunks/framework.DSMn_UDV.js";const g=JSON.parse('{"title":"EpiBranch.jl — Design","description":"","frontmatter":{},"headers":[],"relativePath":"design.md","filePath":"design.md","lastUpdated":null}'),n={name:"design.md"};function o(r,e,d,l,c,h){return i(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="EpiBranch.jl-—-Design" tabindex="-1">EpiBranch.jl — Design <a class="header-anchor" href="#EpiBranch.jl-—-Design" aria-label="Permalink to &quot;EpiBranch.jl — Design {#EpiBranch.jl-—-Design}&quot;">​</a></h1><h2 id="Core-idea" tabindex="-1">Core idea <a class="header-anchor" href="#Core-idea" aria-label="Permalink to &quot;Core idea {#Core-idea}&quot;">​</a></h2><p>The offspring draw is completely decoupled from timing and interventions. The competing risks framework on the transmission hazard is connected naturally to survival analysis (Kenah&#39;s pairwise survival analysis, dynamic survival analysis).</p><h2 id="Architecture" tabindex="-1">Architecture <a class="header-anchor" href="#Architecture" aria-label="Permalink to &quot;Architecture {#Architecture}&quot;">​</a></h2><h3 id="Top-level-diagram" tabindex="-1">Top-level diagram <a class="header-anchor" href="#Top-level-diagram" aria-label="Permalink to &quot;Top-level diagram {#Top-level-diagram}&quot;">​</a></h3><p>A model is the whole generative specification of how data arises: a transmission process, the population it acts on (attributes), the policy in force (interventions), and how cases are observed. The process carries the last three itself, as keyword arguments to its constructor (<code>BranchingProcess(...; interventions, attributes, observation)</code>), rather than through a separate wrapper type. So <code>simulate(model)</code> and <code>loglikelihood(data, model)</code> both work from one object and read them off it. A simulation-based likelihood is then evaluated under the same process that could have produced the data (see <a href="/EpiBranch.jl/dev/design#why-these-live-on-the-model">Why these live on the model</a>).</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>THE MODEL — a TransmissionModel carrying its interventions, attributes, observation</span></span>
<span class="line"><span>─────────</span></span>
<span class="line"><span>TransmissionModel (abstract)</span></span>
<span class="line"><span>├── BranchingProcess(offspring, generation_time; interventions, attributes, observation)</span></span>
<span class="line"><span>└── NetworkProcess(graph, ...;                    interventions, attributes, observation)</span></span>
<span class="line"><span>        core dynamics (offspring law / graph + progression)</span></span>
<span class="line"><span>        + interventions, attributes, observation (each optional)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>OBSERVATION — read off the model, dispatched on its type (alongside AbstractIntervention)</span></span>
<span class="line"><span>─────────</span></span>
<span class="line"><span>ObservationModel (abstract)</span></span>
<span class="line"><span>├── NoObservation                            (default — cases observed exactly)</span></span>
<span class="line"><span>└── PerCaseObservation(detection_prob, delay)</span></span>
<span class="line"><span>    protocol:  apply_observation!(obs, state, rng)   (simulation side)</span></span>
<span class="line"><span>               observe(distribution, obs)            (analytical side)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                       │ simulate(model)   ← reads them from the model</span></span>
<span class="line"><span>                       ▼</span></span>
<span class="line"><span>                 SimulationState (linelist, chain stats, ...)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                       │</span></span>
<span class="line"><span>DATA  ─────────────────┴─────────────►  INFERENCE</span></span>
<span class="line"><span>─────                                   ─────────</span></span>
<span class="line"><span>OffspringCounts                         loglikelihood(data, model)</span></span>
<span class="line"><span>ChainSizes                                ← also reads them from the model</span></span>
<span class="line"><span>ChainLengths</span></span>
<span class="line"><span></span></span>
<span class="line"><span>EXECUTION (given to simulate, not part of the model)</span></span>
<span class="line"><span>─────────</span></span>
<span class="line"><span>rng, n (number of runs), parallel, max_cases/max_generations/max_time (stopping budget)</span></span>
<span class="line"><span>    — these do not change the generative distribution, so they are</span></span>
<span class="line"><span>      arguments to simulate rather than fields of the model</span></span>
<span class="line"><span></span></span>
<span class="line"><span>EXTENSION POINTS (for users adding their own pieces)</span></span>
<span class="line"><span>─────────</span></span>
<span class="line"><span>- Custom transmission model: subtype TransmissionModel, then either</span></span>
<span class="line"><span>  define generate_offspring (offspring-driven) or contacts_of +</span></span>
<span class="line"><span>  collect_exposures=gather_by_target (structure-driven), and</span></span>
<span class="line"><span>  initialise_state (build the starting population with new_state /</span></span>
<span class="line"><span>  add_individuals! / seed!). Add interventions/attributes/observation</span></span>
<span class="line"><span>  accessors for any of those the model carries (each defaults to none);</span></span>
<span class="line"><span>  single_type_offspring/chain_size_distribution for analytics. (A future</span></span>
<span class="line"><span>  HouseholdProcess is exactly this.)</span></span>
<span class="line"><span>- Custom observation model: subtype ObservationModel, define</span></span>
<span class="line"><span>  apply_observation! and observe, dispatched on the observation type</span></span>
<span class="line"><span>  alongside interventions</span></span>
<span class="line"><span>- Custom data type: define a struct + a loglikelihood method</span></span>
<span class="line"><span>- Custom intervention: subtype AbstractIntervention,</span></span>
<span class="line"><span>  define apply_post_transmission!, initialise_individual!,</span></span>
<span class="line"><span>  competing_risk, keep_active, ...</span></span></code></pre></div><h3 id="Why-these-live-on-the-model" tabindex="-1">Why these live on the model <a class="header-anchor" href="#Why-these-live-on-the-model" aria-label="Permalink to &quot;Why these live on the model {#Why-these-live-on-the-model}&quot;">​</a></h3><p>Interventions, attributes, and observation belong to the model rather than to <code>simulate</code>, because a simulation-based likelihood has to reproduce the same generative process that produced the data. If isolation suppressed transmission in the observed outbreak, the likelihood of those chain sizes is only correct if it also applies isolation. Were they <code>simulate</code>-only arguments, you would have to pass an identical stack to both <code>simulate</code> and <code>loglikelihood</code> and keep the two in step by hand, and it is easy to forget. With them on the model, <code>loglikelihood(data, model)</code> already has them.</p><p>This puts them in the <em>generative spec</em>. How interventions compose <em>with each other</em> is a separate question, still handled by the competing-risks machinery (see <a href="/EpiBranch.jl/dev/design#intervention-interface">Intervention interface</a>) and unchanged. A scenario sweep is then a map over models, each scenario a process built with a different policy. This is the usual Turing/SciML idiom.</p><p>Each of the three is read off the model through its own accessor — <code>interventions(model)</code>, <code>attributes(model)</code>, <code>observation(model)</code> — defined once on the abstract <code>TransmissionModel</code> to default to none. A process opts in by carrying a field and defining the matching accessor, so it picks up only what it uses: every process type (<code>BranchingProcess</code>, <code>NetworkProcess</code>, a future <code>HouseholdProcess</code>) gets the engine integration and the simulation-based likelihood without re-implementing them, and a process with no interventions defines no intervention accessor at all. The model is the core dynamics; the three sit on top, each optional.</p><p><code>simulate</code> and <code>loglikelihood</code> take no <code>interventions</code>/<code>attributes</code> arguments: the model is the only source, so the two can never disagree. They are set once, on the constructor — <code>BranchingProcess(...; interventions = [iso])</code>. For a counterfactual, such as a fitted model under a policy it was not fitted with, build a fresh model with the policy you want; there is no in-place &quot;swap one input&quot; helper, so a model&#39;s interventions, attributes and observation are always explicit at construction.</p><p>The four design principles are documented separately (<a href="./principles">Design principles</a>) and are the basis on which this shape is judged.</p><h3 id="Three-separated-stages" tabindex="-1">Three separated stages <a class="header-anchor" href="#Three-separated-stages" aria-label="Permalink to &quot;Three separated stages {#Three-separated-stages}&quot;">​</a></h3><h4 id="1.-Offspring-draw" tabindex="-1">1. Offspring draw <a class="header-anchor" href="#1.-Offspring-draw" aria-label="Permalink to &quot;1. Offspring draw {#1.-Offspring-draw}&quot;">​</a></h4><p>Pure branching process, no time, no interventions.</p><p>For a parent of type <code>j</code>, contacts are drawn from an offspring distribution. This is the classical branching process: a count from a distribution (single-type) or a vector of counts per type (multi-type). The mean (R) and overdispersion (k) come from the offspring distribution. If a contact matrix is provided, the mixing pattern across types is determined by it.</p><h4 id="2.-Timing" tabindex="-1">2. Timing <a class="header-anchor" href="#2.-Timing" aria-label="Permalink to &quot;2. Timing {#2.-Timing}&quot;">​</a></h4><p>Generation times are assigned independently to each contact.</p><p>Each contact is given a generation time sampled from a distribution. That distribution can be fixed, or built per individual by passing a function for <code>generation_time</code>. The engine hands the function the individual and uses the distribution it returns, so the generation time can read any per-individual quantity: the parent&#39;s incubation period (as in the ringbp-style model), or anything else an attributes function has stored. This generation time is the <em>potential</em> time of transmission — the infectiousness profile, or h(t) in survival analysis terms.</p><h4 id="3.-Competing-risks" tabindex="-1">3. Competing risks <a class="header-anchor" href="#3.-Competing-risks" aria-label="Permalink to &quot;3. Competing risks {#3.-Competing-risks}&quot;">​</a></h4><p>Which contacts are actually infected?</p><p>Each contact is resolved independently:</p><ul><li><p>Was the parent isolated before this contact&#39;s generation time? (hazard truncation)</p></li><li><p>Is the contact susceptible? (vaccination, prior immunity, population depletion)</p></li><li><p>Is the parent still infectious? (infectiousness modifier)</p></li><li><p>For leaky isolation: does residual transmission succeed?</p></li></ul><p>Contacts that fail any of these checks are not infected but are still stored. They represent contacts that were made but did not result in transmission. Intervention effort (contacts traced, vaccines administered, tests used) is tracked this way.</p><h3 id="Why-this-separation-matters" tabindex="-1">Why this separation matters <a class="header-anchor" href="#Why-this-separation-matters" aria-label="Permalink to &quot;Why this separation matters {#Why-this-separation-matters}&quot;">​</a></h3><p>Because the offspring distribution is a pure probabilistic object, it can be analysed with standard tools: extinction probability from the dominant eigenvalue, chain size distributions, analytical likelihoods. None of this depends on timing or interventions.</p><p>At the intervention stage, the timing layer is modified, not the offspring layer. The hazard is truncated by isolation. The truncation point is shifted earlier by contact tracing. Susceptibility is modified by vaccination. All of these are competing risks on the same set of potential contacts.</p><p>The generation time CDF evaluated at the intervention time <em>is</em> the survival function. The same mathematical objects appear in simulation and in Kenah&#39;s pairwise likelihood framework. Intervention effectiveness can be estimated from observed generation times using the same quantities used in simulation.</p><p>All contacts are stored, not just successful infections. This gives the contacts table (with <code>infected</code> flag) and intervention effort tracking, without any additional bookkeeping.</p><h2 id="Extension-model" tabindex="-1">Extension model <a class="header-anchor" href="#Extension-model" aria-label="Permalink to &quot;Extension model {#Extension-model}&quot;">​</a></h2><p>New behaviour is added by defining a new type and a method, not by growing options on an existing struct. A user wanting a variant of an existing component should be able to write a small struct plus one or two methods, with no edits to the package source and no copy-pasting of existing function bodies.</p><p>When a field on a public struct is <code>Union{T, U}</code> whose union members trigger different runtime branches, or a <code>Symbol</code> that switches behaviour inside a function body, or a <code>Bool</code> that selects between policies, that is a signal the seam is in the wrong place — turn it into a dispatched-on type instead.</p><p>This applies on every extension axis:</p><ul><li><p><strong>Transmission models</strong>: spatial structure, network structure, and immunity dynamics enter through new <code>TransmissionModel</code> subtypes (or composable wrappers around existing ones), not flags on <code>BranchingProcess</code>.</p></li><li><p><strong>Interventions are orchestrators of smaller dispatched pieces</strong>. An intervention struct (e.g. <code>ContactTracing</code>, <code>Isolation</code>) is a thin shell that wires together independently dispatched components — eligibility, fire-rate, delay, effect. Each component is itself a type with a defined method (e.g. <code>is_eligible(scope, parent, contact, state)</code>). Users extend behaviour by writing new component types. The intervention body itself contains no hardcoded policy branching.</p></li><li><p><strong>Composition works at two levels</strong>: between interventions (the stack carried by the model) and within each intervention (its dispatched pieces).</p></li><li><p><strong>Output, observation, and outcome rules</strong> follow the same shape: mortality, hospitalisation, reporting probability, stopping conditions, line-list columns are typed objects with methods, not closed sets of fields.</p></li></ul><p>The test of correctness for any component is: can a plausible new variant be added without editing the component&#39;s source? If not, the component is doing too much; lift the varying part into a dispatched-on trait.</p><h3 id="Model-interface" tabindex="-1">Model interface <a class="header-anchor" href="#Model-interface" aria-label="Permalink to &quot;Model interface {#Model-interface}&quot;">​</a></h3><p>A transmission model produces candidate transmissions. Timing and infection-resolution happen afterwards, in the engine. How a model plugs in depends on whether it can produce its candidates one parent at a time.</p><p>An <strong>offspring-driven</strong> model (the branching process, and variants like density-dependent or multi-type) implements one method:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>generate_offspring(model, parent, state) -&gt; offspring count</span></span></code></pre></div><p>It returns how many contacts the parent makes: a single count, or a count per type for a multi-type model. The engine&#39;s generic per-generation loop does the rest. For each active parent it calls <code>generate_offspring</code>, creates that many candidate contacts, gives each an infection time from the model&#39;s <code>generation_time</code>, and resolves competing risks. The model assigns no timing, builds no <code>Individual</code>s, and never sees interventions.</p><p>A <strong>structure-driven</strong> model (the network process, and a future household or metapopulation model) cannot produce its candidates one parent at a time. A susceptible may be reachable by several infectious neighbours at once, and infections deplete a fixed pool. Such a model plugs into the same <code>simulate</code> loop by defining <code>initialise_state</code> (set up its fixed population) and <code>contacts_of</code> (the candidate contacts an infectious node reaches each generation), and overriding <code>collect_exposures</code> with <code>gather_by_target</code> so a susceptible reached several times in one generation is resolved once. The engine&#39;s shared timing and competing-risks stages do the rest. <code>NetworkProcess</code> is the worked example.</p><p>Either way the model only says who contacts whom. Timing and the competing-risks decision run the same way for every model. That keeps the offspring layer analysable on its own (extinction probability, chain-size distributions) and keeps the draw differentiable when the offspring distribution permits.</p><h3 id="Three-stages:-one-model-specific,-two-shared" tabindex="-1">Three stages: one model-specific, two shared <a class="header-anchor" href="#Three-stages:-one-model-specific,-two-shared" aria-label="Permalink to &quot;Three stages: one model-specific, two shared {#Three-stages:-one-model-specific,-two-shared}&quot;">​</a></h3><p>Of the three stages, only the first is model-specific:</p><ol><li><p><strong>Candidate generation (model-specific).</strong> The branching process generates the tree: each parent&#39;s offspring become new candidate nodes. The network process generates nothing: its contact structure is an input (a fixed adjacency), and its candidates are the susceptible neighbours of each infectious node. So tree generation belongs to the branching process and has no counterpart for networks.</p></li><li><p><strong>Timing (shared).</strong> The engine gives each candidate an infection time from the parent&#39;s <code>generation_time</code>, the same way for both processes. The <code>generation_time</code> distribution is model data; drawing and assigning the times is an engine stage.</p></li><li><p><strong>Competing risks (shared).</strong> The engine resolves each candidate to infected or not by one per-pair decision composed from a list of risk sources. Parent infectiousness and contact susceptibility are not privileged engine checks: they are default risk sources on the same <code>competing_risk</code> surface an intervention uses, composed as <code>[built-ins; interventions]</code> through one shared path. Isolation truncation and any risk an intervention contributes join the same list.</p></li></ol><p>Only candidate generation differs, so timing and the competing-risks decision are primitives both processes call. The two still keep separate top-level loops: the network loop also has to resolve several infectious neighbours competing for one susceptible node, which the branching process never produces. But both sit over the same engine primitives, in sibling modules. To add another process (household- or metapopulation-structured, say), supply a new candidate-generation step and reuse the shared timing and competing-risks stages.</p><h3 id="Naming-convention-for-abstract-types" tabindex="-1">Naming convention for abstract types <a class="header-anchor" href="#Naming-convention-for-abstract-types" aria-label="Permalink to &quot;Naming convention for abstract types {#Naming-convention-for-abstract-types}&quot;">​</a></h3><p>The package follows the same role-vs-thing split that <code>Base</code> and <code>Distributions.jl</code> use:</p><ul><li><p><strong>Prefix with <code>Abstract</code></strong> when the type names <em>what&#39;s being abstracted</em> and the bare noun would read as a concrete value. The main-axis abstractions a user composes between fall here: <code>AbstractIntervention</code>, <code>AbstractVaccination</code>, <code>AbstractEffectMode</code>, <code>AbstractClinicalTransition</code>, <code>AbstractStoppingRule</code>. Same convention as <code>AbstractFloat</code>, <code>AbstractArray</code>, <code>AbstractDict</code>.</p></li><li><p><strong>Drop the prefix</strong> when the type already names a <em>role</em> or <em>concept</em>: <code>TransmissionModel</code>, <code>ObservationModel</code>, and the per-intervention seam traits (<code>IsolationEligibility</code>, <code>TraceEligibility</code>, <code>TraceRate</code>, <code>TraceDelay</code>, <code>TraceAction</code>). Same convention as <code>Number</code>, <code>Function</code>, <code>Distribution</code>.</p></li></ul><p>Per-intervention seam traits should be qualified with the intervention&#39;s name (e.g. <code>TraceEligibility</code>, not <code>Eligibility</code>) so the type reads unambiguously in user code and doesn&#39;t collide with other interventions&#39; versions of the same trait.</p><h2 id="Individual-state" tabindex="-1">Individual state <a class="header-anchor" href="#Individual-state" aria-label="Permalink to &quot;Individual state {#Individual-state}&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Individual (struct)</span></span>
<span class="line"><span>├── Core (used by the engine)</span></span>
<span class="line"><span>│   ├── id, parent_id, generation, chain_id    # transmission tree</span></span>
<span class="line"><span>│   ├── infection_time                          # timing</span></span>
<span class="line"><span>│   ├── susceptibility, infectiousness          # universal modifiers</span></span>
<span class="line"><span>│   └── secondary_case_ids                      # filled during simulation</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>└── state::Dict{Symbol, Any}                    # everything else</span></span></code></pre></div><p>Only <code>susceptibility</code>, <code>infectiousness</code>, and <code>infection_time</code> are read by the engine. The dict carries everything else and is the deliberate extension hatch — interventions, attributes builders, transitions, and observation models each own a small set of keys. The engine never inspects them.</p><p>Why a dict instead of typed slots on <code>Individual</code>: a typed core would couple the struct to every intervention&#39;s state shape, and would break <code>RingVaccination</code> / <code>MassVaccination</code>&#39;s dose-label namespacing (the keys are <code>:vaccinated_&lt;label&gt;</code>, so they cannot collapse to a single typed field). The dict keeps <code>Individual</code> independent of which interventions a user composes.</p><h3 id="Accessor-convention" tabindex="-1">Accessor convention <a class="header-anchor" href="#Accessor-convention" aria-label="Permalink to &quot;Accessor convention {#Accessor-convention}&quot;">​</a></h3><p>Read via a one-line wrapper that pins the result type and supplies the safe default — <code>onset_time(ind) = get(ind.state, :onset_time, NaN)::Float64</code>. New code should not call <code>get(ind.state, …)</code> directly; add an accessor in <code>src/state_accessors.jl</code> instead.</p><h3 id="Reserved-keys" tabindex="-1">Reserved keys <a class="header-anchor" href="#Reserved-keys" aria-label="Permalink to &quot;Reserved keys {#Reserved-keys}&quot;">​</a></h3><p>The keys below are reserved by the package. Custom interventions should pick names that do not collide.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Key</th><th style="text-align:right;">Type</th><th style="text-align:right;">Default</th><th style="text-align:right;">Owner</th><th style="text-align:right;">When set</th></tr></thead><tbody><tr><td style="text-align:right;"><code>:infected</code></td><td style="text-align:right;"><code>Bool</code></td><td style="text-align:right;"><code>true</code></td><td style="text-align:right;">Engine</td><td style="text-align:right;">Competing-risks resolution</td></tr><tr><td style="text-align:right;"><code>:type</code></td><td style="text-align:right;"><code>Int</code></td><td style="text-align:right;"><code>1</code></td><td style="text-align:right;">Engine (multi-type)</td><td style="text-align:right;">Contact creation</td></tr><tr><td style="text-align:right;"><code>:onset_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;"><code>NaN</code></td><td style="text-align:right;"><code>clinical_presentation</code></td><td style="text-align:right;">Init</td></tr><tr><td style="text-align:right;"><code>:asymptomatic</code></td><td style="text-align:right;"><code>Bool</code></td><td style="text-align:right;"><code>false</code></td><td style="text-align:right;"><code>clinical_presentation</code></td><td style="text-align:right;">Init</td></tr><tr><td style="text-align:right;"><code>:age</code></td><td style="text-align:right;"><code>Real</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>demographics</code></td><td style="text-align:right;">Init</td></tr><tr><td style="text-align:right;"><code>:sex</code></td><td style="text-align:right;"><code>Symbol</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>demographics</code></td><td style="text-align:right;">Init</td></tr><tr><td style="text-align:right;"><code>:risk_group</code></td><td style="text-align:right;"><code>Symbol</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>demographics</code></td><td style="text-align:right;">Init</td></tr><tr><td style="text-align:right;"><code>:isolated</code></td><td style="text-align:right;"><code>Bool</code></td><td style="text-align:right;"><code>false</code></td><td style="text-align:right;"><code>Isolation</code></td><td style="text-align:right;"><code>resolve_individual!</code></td></tr><tr><td style="text-align:right;"><code>:isolation_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;"><code>Inf</code></td><td style="text-align:right;"><code>Isolation</code></td><td style="text-align:right;"><code>resolve_individual!</code></td></tr><tr><td style="text-align:right;"><code>:test_positive</code></td><td style="text-align:right;"><code>Bool</code></td><td style="text-align:right;"><code>false</code></td><td style="text-align:right;"><code>Isolation</code></td><td style="text-align:right;"><code>resolve_individual!</code></td></tr><tr><td style="text-align:right;"><code>:traced</code></td><td style="text-align:right;"><code>Bool</code></td><td style="text-align:right;"><code>false</code></td><td style="text-align:right;"><code>ContactTracing</code></td><td style="text-align:right;"><code>apply_post_transmission!</code></td></tr><tr><td style="text-align:right;"><code>:quarantined</code></td><td style="text-align:right;"><code>Bool</code></td><td style="text-align:right;"><code>false</code></td><td style="text-align:right;"><code>ContactTracing</code></td><td style="text-align:right;"><code>apply_post_transmission!</code></td></tr><tr><td style="text-align:right;"><code>:traced_isolation_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;"><code>Inf</code></td><td style="text-align:right;"><code>ContactTracing</code> → <code>Isolation</code></td><td style="text-align:right;">Internal handoff</td></tr><tr><td style="text-align:right;"><code>:trace_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>ContactTracing</code> (<code>depth &gt; 1</code>)</td><td style="text-align:right;"><code>apply_post_transmission!</code></td></tr><tr><td style="text-align:right;"><code>:ring_remaining</code></td><td style="text-align:right;"><code>Int</code></td><td style="text-align:right;"><code>0</code></td><td style="text-align:right;"><code>ContactTracing</code> (<code>depth &gt; 1</code>)</td><td style="text-align:right;"><code>apply_post_transmission!</code></td></tr><tr><td style="text-align:right;"><code>:vaccinated[_&lt;label&gt;]</code></td><td style="text-align:right;"><code>Bool</code></td><td style="text-align:right;"><code>false</code></td><td style="text-align:right;"><code>AbstractVaccination</code></td><td style="text-align:right;">Init / <code>apply_post_transmission!</code></td></tr><tr><td style="text-align:right;"><code>:vaccination_time[_&lt;label&gt;]</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;"><code>Inf</code></td><td style="text-align:right;"><code>AbstractVaccination</code></td><td style="text-align:right;"><code>apply_post_transmission!</code></td></tr><tr><td style="text-align:right;"><code>:vaccine_efficacy[_&lt;label&gt;]</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>AbstractVaccination</code></td><td style="text-align:right;"><code>apply_post_transmission!</code></td></tr><tr><td style="text-align:right;"><code>:reporting_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;"><code>Inf</code></td><td style="text-align:right;"><code>Reporting</code> transition</td><td style="text-align:right;"><code>resolve_individual!</code></td></tr><tr><td style="text-align:right;"><code>:admitted</code></td><td style="text-align:right;"><code>Bool</code></td><td style="text-align:right;"><code>false</code></td><td style="text-align:right;"><code>Hospitalisation</code> transition</td><td style="text-align:right;"><code>resolve_individual!</code></td></tr><tr><td style="text-align:right;"><code>:admission_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;"><code>Inf</code></td><td style="text-align:right;"><code>Hospitalisation</code> transition</td><td style="text-align:right;"><code>resolve_individual!</code></td></tr><tr><td style="text-align:right;"><code>:death_candidate_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;"><code>Inf</code></td><td style="text-align:right;"><code>Outcome</code> transition</td><td style="text-align:right;"><code>resolve_individual!</code></td></tr><tr><td style="text-align:right;"><code>:recovery_candidate_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;"><code>Inf</code></td><td style="text-align:right;"><code>Outcome</code> transition</td><td style="text-align:right;"><code>resolve_individual!</code></td></tr><tr><td style="text-align:right;"><code>:outcome</code></td><td style="text-align:right;"><code>Symbol</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>Outcome</code> transition</td><td style="text-align:right;"><code>resolve_individual!</code> (terminal)</td></tr><tr><td style="text-align:right;"><code>:outcome_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>Outcome</code> transition</td><td style="text-align:right;"><code>resolve_individual!</code> (terminal)</td></tr><tr><td style="text-align:right;"><code>:reported</code></td><td style="text-align:right;"><code>Bool</code></td><td style="text-align:right;"><code>false</code></td><td style="text-align:right;"><code>PerCaseObservation</code> <em>or</em> <code>Reporting</code> transition</td><td style="text-align:right;">Post-simulation projection / <code>resolve_individual!</code></td></tr><tr><td style="text-align:right;"><code>:report_time</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>PerCaseObservation</code></td><td style="text-align:right;">Post-simulation projection</td></tr><tr><td style="text-align:right;"><code>:cluster_theta</code></td><td style="text-align:right;"><code>Float64</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><code>ClusterMixed</code> analytics</td><td style="text-align:right;">First simulation read</td></tr></tbody></table><p>The vaccination keys are namespaced by <code>dose_label</code> — the default label writes to plain <code>:vaccinated</code> / <code>:vaccination_time</code> / <code>:vaccine_efficacy</code>; any other label suffixes the key (so <code>dose_label = :boost</code> writes to <code>:vaccinated_boost</code> etc.). This lets multi-dose schedules compose without colliding.</p><p><code>:reported</code> is shared between the <code>Reporting</code> clinical transition (which sets it from a probability gate) and <code>PerCaseObservation</code> (which sets it post-simulation from a detection-probability draw). Composing both in the same simulation is not supported — they will overwrite each other.</p><h3 id="Naming-convention-for-downstream-packages" tabindex="-1">Naming convention for downstream packages <a class="header-anchor" href="#Naming-convention-for-downstream-packages" aria-label="Permalink to &quot;Naming convention for downstream packages {#Naming-convention-for-downstream-packages}&quot;">​</a></h3><p>Built-in keys use short bare names like <code>:isolated</code>, <code>:traced</code>, <code>:age</code>. Those names are reserved. If you&#39;re adding keys from another package, prefix them with a short tag for your package so they don&#39;t collide with built-ins or with keys other packages might add.</p><h2 id="Intervention-interface" tabindex="-1">Intervention interface <a class="header-anchor" href="#Intervention-interface" aria-label="Permalink to &quot;Intervention interface {#Intervention-interface}&quot;">​</a></h2><p>Five hooks, all optional. See the <a href="/EpiBranch.jl/dev/tutorials/extending#Extending-EpiBranch">Extending guide</a> for the full input/output contract, ordering guarantees, and a <code>BorderClosure</code> worked example.</p><ul><li><p><code>initialise_individual!(intervention, individual, state)</code> — set up fields on a new contact</p></li><li><p><code>resolve_individual!(intervention, individual, state)</code> — determine intervention state before transmission (e.g. compute isolation time from onset + delay)</p></li><li><p><code>apply_post_transmission!(intervention, state, new_contacts)</code> — act on contacts after creation (e.g. contact tracing, ring vaccination). All contacts, infected and non-infected, are passed.</p></li><li><p><code>competing_risk(intervention, parent, contact, state)</code> — return the <code>Risk</code> (or <code>NTuple{N, Risk}</code>) this intervention contributes against the parent → contact transmission, or <code>nothing</code>.</p></li><li><p><code>keep_active(intervention, state, targets, is_new)</code> — return the ids of this generation&#39;s contacts to keep generating contacts into the next generation, beyond the newly infected cases. Lets an intervention grow the graph from uninfected nodes, e.g. contact tracing reaching contacts-of-contacts. Default: none.</p></li></ul><p>Interventions are stacked in a vector and applied in order. Each intervention has its own fields on the individual and declares what fields it requires.</p><h3 id="Time-based-scheduling" tabindex="-1">Time-based scheduling <a class="header-anchor" href="#Time-based-scheduling" aria-label="Permalink to &quot;Time-based scheduling {#Time-based-scheduling}&quot;">​</a></h3><p>Time-based scheduling is provided by the <code>Scheduled</code> wrapper. Individual interventions do not carry their own <code>start_time</code> — they are wrapped with <code>Scheduled(iv; start_time = ...)</code> (and/or <code>end_time</code>, <code>start_after_cases</code>, or a custom predicate). <code>Scheduled</code> enforces start times at two levels:</p><ul><li><p>A population-level gate (<code>is_active</code>) skips the inner <code>resolve_individual!</code> and <code>apply_post_transmission!</code> until the condition is met.</p></li><li><p>Once active, <code>Scheduled</code> performs individual-level reset: after each per-individual hook, if the individual&#39;s <code>intervention_time</code> falls before <code>start_time</code>, it calls <code>reset!</code> on the inner intervention.</p></li></ul><p>Each schedulable intervention declares:</p><ul><li><p><code>intervention_time(intervention, individual)</code> — the time at which the effect occurs (e.g. isolation time, trace time). Default: <code>-Inf</code> (always applies).</p></li><li><p><code>reset!(intervention, individual)</code> — reverts all state changes made by the intervention. Default: no-op.</p></li></ul><p>The check is on <em>action time</em>, not infection time. An individual infected before the policy start can still be affected if their computed action time (e.g. <code>onset + delay</code>) falls after <code>start_time</code>. This is the correct competing-risk interpretation: the testing infrastructure must be available at the time the individual would be tested.</p><h2 id="Transmission-modifiers" tabindex="-1">Transmission modifiers <a class="header-anchor" href="#Transmission-modifiers" aria-label="Permalink to &quot;Transmission modifiers {#Transmission-modifiers}&quot;">​</a></h2><p>All interventions ultimately map onto two numbers:</p><ul><li><p><code>susceptibility</code> (0–1): probability of being infected given exposure. Reduced by vaccination, prior immunity, or population depletion.</p></li><li><p><code>infectiousness</code> (0–1): modifier on onward transmission. Reduced by isolation (via hazard truncation), treatment, or asymptomatic status.</p></li></ul><p>For a contact to be infected, it must survive the parent&#39;s infectiousness check AND the contact&#39;s susceptibility check AND the timing check (generation time vs isolation time).</p><h2 id="Multi-type-branching-processes" tabindex="-1">Multi-type branching processes <a class="header-anchor" href="#Multi-type-branching-processes" aria-label="Permalink to &quot;Multi-type branching processes {#Multi-type-branching-processes}&quot;">​</a></h2><p>Multiple types (age groups, risk groups, spatial patches) are supported in the offspring draw. For a parent of type <code>j</code>, offspring counts per type are drawn from a joint distribution. The mixing pattern comes from a contact matrix; the count distribution comes from the offspring distribution family.</p><p>Each contact is allocated to a type, stored as <code>:type</code> in its state dict. Interventions and output are unchanged — they operate on individual-level state, not types.</p><h2 id="Composing-analytical-methods" tabindex="-1">Composing analytical methods <a class="header-anchor" href="#Composing-analytical-methods" aria-label="Permalink to &quot;Composing analytical methods {#Composing-analytical-methods}&quot;">​</a></h2><p>The simulation side is extended by implementing the intervention protocol. The analytical side uses a different pattern. Each extension is a Julia type with a method on <code>chain_size_distribution</code> or <code>loglikelihood</code>, or both. There is no new abstract type beyond the existing <code>TransmissionModel</code>; multiple dispatch handles the rest.</p><h3 id="Two-kinds-of-extension" tabindex="-1">Two kinds of extension <a class="header-anchor" href="#Two-kinds-of-extension" aria-label="Permalink to &quot;Two kinds of extension {#Two-kinds-of-extension}&quot;">​</a></h3><p>Offspring specifications replace what a branching process draws per individual. An example is <code>ClusterMixed(build, mixing)</code>, which lets the offspring distribution&#39;s parameters vary from chain to chain. These are stored in <code>BranchingProcess.offspring</code> and participate via:</p><ul><li><p><code>draw_offspring(rng, offspring_spec, individual, state)</code> for simulation</p></li><li><p><code>chain_size_distribution(offspring_spec)</code> for analytics (returns a distribution)</p></li></ul><p>Observation models capture how the latent process generates data. They subtype <code>ObservationModel</code> and are carried by the process the same way interventions are: pass <code>observation = …</code> to the constructor. An observation joins in through two methods dispatched on its type:</p><ul><li><p><code>observe(base_distribution, ::YourObservation)</code> returning a distribution that transforms the latent chain-size distribution (e.g. <code>ThinnedChainSize(base, ρ)</code>) — the analytical side</p></li><li><p><code>apply_observation!(::YourObservation, state, rng)</code> marking observed cases on a finished simulation — the simulation side</p></li></ul><p>The engine and the likelihood read <code>_observation(model)</code> and dispatch on it, so neither <code>simulate</code> nor <code>loglikelihood</code> needs to know which observation is in play.</p><h3 id="Why-observation-models-return-distributions" tabindex="-1">Why observation models return distributions <a class="header-anchor" href="#Why-observation-models-return-distributions" aria-label="Permalink to &quot;Why observation models return distributions {#Why-observation-models-return-distributions}&quot;">​</a></h3><p><code>observe(base, ::PerCaseObservation)</code> returns <code>ThinnedChainSize(base, ρ)</code>. The result is itself a <code>DiscreteUnivariateDistribution</code> with its own <code>logpdf</code>, so it goes through the same likelihood path as the latent law: <code>loglikelihood(data, model)</code> evaluates <code>logpdf</code> on whatever <code>observe</code> returns, and no observation needs its own likelihood method.</p><p>If instead the thinning were computed inside a per-observation <code>loglikelihood</code> method, every new observation type would need its joint likelihood with the latent law worked out by hand. Returning a distribution means each observation is written once, as a transform.</p><h3 id="Closed-forms-as-dispatch-optimisations" tabindex="-1">Closed forms as dispatch optimisations <a class="header-anchor" href="#Closed-forms-as-dispatch-optimisations" aria-label="Permalink to &quot;Closed forms as dispatch optimisations {#Closed-forms-as-dispatch-optimisations}&quot;">​</a></h3><p>Some combinations of offspring and mixing have closed forms. Poisson offspring with a Gamma-distributed rate gives <code>PoissonGammaChainSize</code>, which is the <code>gborel</code> likelihood from <code>epichains</code>. These plug in by specialising <code>chain_size_distribution</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chain_size_distribution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ClusterMixed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ChainSizeMixture</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">build, o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mixing)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chain_size_distribution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ClusterMixed{PoissonFamily, &lt;:Gamma}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PoissonGammaChainSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The generic case uses adaptive Gauss-Kronrod quadrature via <code>ChainSizeMixture</code>. When a closed form applies, dispatch uses it without the user having to ask.</p><h2 id="Simulation-and-inference" tabindex="-1">Simulation and inference <a class="header-anchor" href="#Simulation-and-inference" aria-label="Permalink to &quot;Simulation and inference {#Simulation-and-inference}&quot;">​</a></h2><p>The simulation engine works in place, one generation at a time. Each generation it resolves intervention state on every active parent, gathers the generation&#39;s candidate contacts (via <code>collect_exposures</code>), initialises intervention state on each new contact, runs the post-transmission hooks, and resolves competing risks to set <code>:infected</code>. A model varies only how it names those contacts, through one of two seams — neither of which takes an <code>interventions</code> argument. Offspring-driven models (the tree case) define <code>generate_offspring(model, parent, state)</code>, returning a pure offspring count: the engine creates that many fresh contacts and gives each an infection time from the model&#39;s <code>generation_time</code>, so the model builds no <code>Individual</code>s and assigns no timing. Structure-driven models (the graph case) define <code>contacts_of(model, node, state)</code>, returning <code>(contact, infection_time)</code> pairs over existing nodes, and override <code>collect_exposures</code> with <code>gather_by_target</code> so a node reached by several neighbours in one generation resolves once. Either way the engine owns the downstream stages. Copying the whole state every generation would be far too expensive for an unbounded tree, so the engine mutates in place on purpose.</p><h3 id="Analytical-likelihoods" tabindex="-1">Analytical likelihoods <a class="header-anchor" href="#Analytical-likelihoods" aria-label="Permalink to &quot;Analytical likelihoods {#Analytical-likelihoods}&quot;">​</a></h3><p><code>loglikelihood(ChainSizes(data), Poisson(R))</code> and similar analytical methods are deterministic scalar functions of the parameters. They work with any AD backend (ForwardDiff, Enzyme, Mooncake, ReverseDiff) and with gradient-based samplers like NUTS.</p><h3 id="Simulation-based-likelihoods" tabindex="-1">Simulation-based likelihoods <a class="header-anchor" href="#Simulation-based-likelihoods" aria-label="Permalink to &quot;Simulation-based likelihoods {#Simulation-based-likelihoods}&quot;">​</a></h3><p><code>loglikelihood(ChainSizes(data), model)</code> runs stochastic simulations internally whenever the model carries a non-empty intervention stack. Because the simulation draws random numbers, the output is a noisy estimate of the true likelihood, not a smooth function of the parameters. Gradients of a single stochastic realisation are not useful estimates of the gradient of the expected likelihood. Gradient-based samplers (NUTS, HMC) should not be used here.</p><p>Use gradient-free samplers instead: Metropolis-Hastings (<code>MH()</code>), particle methods, or similar. The inference tutorial demonstrates this with <code>MH()</code>.</p><h3 id="Sim-analytical-consistency" tabindex="-1">Sim ↔ analytical consistency <a class="header-anchor" href="#Sim-analytical-consistency" aria-label="Permalink to &quot;Sim ↔ analytical consistency {#Sim-analytical-consistency}&quot;">​</a></h3><p>An extension with both an analytical chain size distribution and a simulation path should have a regression test confirming they agree. The test suite has a helper at <code>test/testutils/sim_analytical_consistency.jl</code>. A new type plugs in by defining two methods:</p><ul><li><p><code>generative_model(m)</code> strips observation wrappers so <code>simulate(model, n)</code> can run</p></li><li><p><code>observe_chain_sizes(m, true_sizes, rng)</code> transforms simulated true chain sizes into observed ones (defaults to the identity; observation models override it)</p></li></ul><p><code>sim_analytical_consistent(model; n_chains, sizes, rng)</code> then simulates, applies <code>observe_chain_sizes</code>, and compares the empirical PMF against <code>chain_size_distribution(model)</code>. The helper already covers bare offspring, <code>ClusterMixed</code>, and models carrying a <code>PerCaseObservation</code>.</p><h2 id="Connection-to-survival-analysis" tabindex="-1">Connection to survival analysis <a class="header-anchor" href="#Connection-to-survival-analysis" aria-label="Permalink to &quot;Connection to survival analysis {#Connection-to-survival-analysis}&quot;">​</a></h2><p>The generation time distribution g(t) = h(t)/R is the normalised infectiousness profile. Its CDF, G(t), is the cumulative hazard. When isolation occurs at time t_iso, the probabilities are:</p><ul><li><p>P(transmission before isolation) = G(t_iso)</p></li><li><p>P(transmission after isolation) = 1 - G(t_iso)</p></li></ul><p>The result is right-censoring of the transmission process. The generation time distribution (derived from the hazard) is connected to the population growth rate through the Euler-Lotka equation R = 1/M_g(-r).</p><p>The same objects – the generation time distribution and the censoring time – appear in both simulation and Kenah&#39;s pairwise likelihood. Inference built on top of this framework would fit the same quantities we simulate from.</p><h2 id="The-host-timeline-and-transmission-route-windows" tabindex="-1">The host timeline and transmission-route windows <a class="header-anchor" href="#The-host-timeline-and-transmission-route-windows" aria-label="Permalink to &quot;The host timeline and transmission-route windows {#The-host-timeline-and-transmission-route-windows}&quot;">​</a></h2><p>The three stages above describe the simplest model: one offspring law, one generation-time distribution. On top of that, the branching process carries a <strong>host timeline</strong> (the case&#39;s natural history) and <strong>transmission routes</strong> (infectiousness windows) that key off it. This is what lets the package say &quot;infectiousness begins only after a latent period&quot;, &quot;a second route runs at the funeral, between death and burial&quot;, &quot;hospital-acquired transmission runs between admission and discharge&quot;, or &quot;isolation lowers R by cutting the infectious period short&quot;: each as one mechanism rather than a special case. The three-stage engine and the single-window analytical layer are unchanged; the timeline and the windows live in stages 1–3 without disturbing them.</p><h3 id="The-model-carries-a-natural-history" tabindex="-1">The model carries a natural history <a class="header-anchor" href="#The-model-carries-a-natural-history" aria-label="Permalink to &quot;The model carries a natural history {#The-model-carries-a-natural-history}&quot;">​</a></h3><p>A case has a timeline of timed states from infection: <code>:infectious</code> (onset of infectiousness), <code>:onset</code> (symptoms), <code>:severe</code>, <code>:died</code> or <code>:recovered</code>, <code>:buried</code>. Each is a transition with a <code>from</code> state it follows, a delay, and optionally a probability (it happens for a fraction of cases) or a terminal flag (it ends the case). This is the existing <code>AbstractClinicalTransition</code> surface generalised so the user names the state and can start from <code>:infection</code>, with incubation and the latent period becoming ordinary transitions rather than special cases.</p><p>The natural history is the process&#39;s <code>progression</code> (a field on <code>BranchingProcess</code>). It is biology, the same kind of object as the offspring law, but it is not the part that creates the tree. It sits alongside the transmission routes and apart from interventions (policy) and attributes (population heterogeneity). The three concerns separate cleanly, even though a model carries all three:</p><ul><li><p><strong>process</strong> — how the disease spreads and progresses (transmission routes + <code>progression</code>);</p></li><li><p><strong>interventions</strong> — what is done about it (isolation, vaccination, tracing);</p></li><li><p><strong>attributes</strong> — who the people are (age, susceptibility).</p></li></ul><p>Keeping them as distinct concerns is what lets a model compose them without entangling them: the process knows nothing about the policy, and the policy is the same object whichever process it acts on.</p><h3 id="Transmission-is-a-set-of-route-windows" tabindex="-1">Transmission is a set of route windows <a class="header-anchor" href="#Transmission-is-a-set-of-route-windows" aria-label="Permalink to &quot;Transmission is a set of route windows {#Transmission-is-a-set-of-route-windows}&quot;">​</a></h3><p>A transmission route is a window on the timeline: an offspring law, a <code>from</code> state where infectiousness begins, the <code>until</code> states that end it, and a survival kernel for the timing within it.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Infectiousness</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NegBin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); from </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :infectious</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, until </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:recovered</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:died</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:isolated</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), kernel </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Weibull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># community</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Infectiousness</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NegBin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); from </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :died</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,       until </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:buried</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,),                     kernel </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Weibull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># funeral</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Infectiousness</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NegBin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); from </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :admitted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,   until </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:discharged</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:died</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),           kernel </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Weibull</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># hospital-acquired</span></span></code></pre></div><p>The funeral is nothing special: it is one route among many. Hospital-acquired (nosocomial) transmission is the same object keyed to <code>:admitted</code> and <code>:discharged</code>; a vector-borne or sexual route would be others. The bare <code>BranchingProcess(NegBin(R, k), kernel)</code> is one route with <code>from = :infection</code> and no censoring, which is the simplest model unchanged. The kernel is a survival distribution: every continuous time-to-event distribution has a survival function and a hazard, so its shape sets the infectiousness profile (when transmission concentrates within the window) and its survival function does the censoring.</p><h3 id="The-three-stages-still-hold" tabindex="-1">The three stages still hold <a class="header-anchor" href="#The-three-stages-still-hold" aria-label="Permalink to &quot;The three stages still hold {#The-three-stages-still-hold}&quot;">​</a></h3><p>The generalisation lands entirely in stages 2 and 3; stage 1 stays the pure, analysable layer.</p><ol><li><p><strong>Branch (stage 1).</strong> For each infector, draw each window&#39;s offspring from its own law and tag every contact with its window. No timing or interventions are read. The intrinsic offspring is the sum of the per-window laws; for one window it is exactly today&#39;s <code>NegBin(R, k)</code> with every closed form intact, and for several it is the product of their generating functions.</p></li><li><p><strong>Timing (stage 2).</strong> A contact&#39;s time is its window&#39;s <code>from</code>-state time plus a draw from the window&#39;s kernel. The latent period is just the offset to <code>:infectious</code>, and the generation interval of the next case is the latent period plus the kernel draw, derived rather than specified.</p></li><li><p><strong>Censor (stage 3).</strong> Each window contributes a full-block competing risk at the earliest of its <code>until</code> states. Because a contact is infected only if no risk blocks it, the earliest removal before the contact&#39;s time wins, with no min-logic to write. This is the shape isolation already has, a full block at a removal time, so isolation stops being special and becomes one removal state among death, recovery, and burial. Susceptibility and vaccination compose on the same surface.</p></li></ol><p>R and k stay the inputs. R is the intrinsic reproduction number, the offspring a case would make if never removed, and the realised R falls out of the censoring: shortening the infectious window (isolation, safe burial) blocks more contacts and lowers it, mechanically. k is the intrinsic offspring dispersion, a per-infector frailty. It is deliberately not the shape of the infectious period: drawing the count from the duration would couple stage 1 to timing and break the branch-first decoupling the whole engine rests on.</p><h3 id="Windows-open-at-their-from-state" tabindex="-1">Windows open at their <code>from</code> state <a class="header-anchor" href="#Windows-open-at-their-from-state" aria-label="Permalink to &quot;Windows open at their \`from\` state {#Windows-open-at-their-from-state}&quot;">​</a></h3><p>A window contributes contacts only once its <code>from</code> state has occurred for the infector. The community window&#39;s <code>:infectious</code> is structural and always occurs; the funeral window&#39;s <code>:died</code> occurs only for those who die, so &quot;funeral transmission for the dead alone&quot; falls out of the general rule rather than a special case. A survivor never materialises funeral contacts, so nothing is created only to be censored.</p><p>The offspring law that drives outbreak size is therefore a fate-mixture: community offspring for everyone, plus funeral offspring for the fraction who die. That mixture is inherent to funeral transmission, not an artefact of the draw order. Analytically it is a mixture of the per-window laws, closed-form when each branch is a <code>NegBin</code> and simulation otherwise, so the analytical/simulation dual path carries over.</p><h3 id="What-this-subsumes" tabindex="-1">What this subsumes <a class="header-anchor" href="#What-this-subsumes" aria-label="Permalink to &quot;What this subsumes {#What-this-subsumes}&quot;">​</a></h3><p>The latent period becomes the <code>:infection → :infectious</code> transition; the contact interval becomes the community window&#39;s kernel; the generation interval is derived from the two. Isolation&#39;s truncation becomes a removal state in a window&#39;s <code>until</code>. So a set of currently separate mechanisms (the generation time, isolation truncation, clinical transitions, and the missing funeral and severity machinery) collapse onto one timeline that transmission, interventions, and the natural history all read.</p><h2 id="References" tabindex="-1">References <a class="header-anchor" href="#References" aria-label="Permalink to &quot;References {#References}&quot;">​</a></h2><ul><li><p>Kenah E, Lipsitch M, Robins JM (2008). Generation interval contraction and epidemic data analysis. <em>Mathematical Biosciences</em> 213(1):71–79. <a href="https://doi.org/10.1016/j.mbs.2008.02.007" target="_blank" rel="noreferrer">doi:10.1016/j.mbs.2008.02.007</a></p></li><li><p>Kenah E (2011). Contact intervals, survival analysis of epidemic data, and estimation of R0. <em>Biostatistics</em> 12(3):548–566. <a href="https://doi.org/10.1093/biostatistics/kxq068" target="_blank" rel="noreferrer">doi:10.1093/biostatistics/kxq068</a></p></li><li><p>KhudaBukhsh WR, Choi B, Kenah E, Rempala GA (2020). Survival dynamical systems: individual-level survival analysis from population-level epidemic models. <em>Interface Focus</em> 10(1):20190048. <a href="https://doi.org/10.1098/rsfs.2019.0048" target="_blank" rel="noreferrer">doi:10.1098/rsfs.2019.0048</a></p></li><li><p>Wallinga J, Lipsitch M (2007). How generation intervals shape the relationship between growth rates and reproductive numbers. <em>Proceedings of the Royal Society B</em> 274(1609):599–604. <a href="https://doi.org/10.1098/rspb.2006.3754" target="_blank" rel="noreferrer">doi:10.1098/rspb.2006.3754</a></p></li></ul>`,134)])])}const u=t(n,[["render",o]]);export{g as __pageData,u as default};
