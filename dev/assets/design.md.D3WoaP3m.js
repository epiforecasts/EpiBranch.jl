import{_ as e,o as a,c as t,aB as s}from"./chunks/framework.BTFJqP47.js";const u=JSON.parse('{"title":"EpiBranch.jl — Design","description":"","frontmatter":{},"headers":[],"relativePath":"design.md","filePath":"design.md","lastUpdated":null}'),n={name:"design.md"};function o(r,i,l,d,c,h){return a(),t("div",null,[...i[0]||(i[0]=[s(`<h1 id="EpiBranch.jl-—-Design" tabindex="-1">EpiBranch.jl — Design <a class="header-anchor" href="#EpiBranch.jl-—-Design" aria-label="Permalink to &quot;EpiBranch.jl — Design {#EpiBranch.jl-—-Design}&quot;">​</a></h1><h2 id="Core-idea" tabindex="-1">Core idea <a class="header-anchor" href="#Core-idea" aria-label="Permalink to &quot;Core idea {#Core-idea}&quot;">​</a></h2><p>The offspring draw is completely decoupled from timing and interventions. The competing risks framework on the transmission hazard is connected naturally to survival analysis (Kenah&#39;s pairwise survival analysis, dynamic survival analysis).</p><h2 id="Architecture" tabindex="-1">Architecture <a class="header-anchor" href="#Architecture" aria-label="Permalink to &quot;Architecture {#Architecture}&quot;">​</a></h2><h3 id="Three-separated-stages" tabindex="-1">Three separated stages <a class="header-anchor" href="#Three-separated-stages" aria-label="Permalink to &quot;Three separated stages {#Three-separated-stages}&quot;">​</a></h3><h4 id="1.-Offspring-draw" tabindex="-1">1. Offspring draw <a class="header-anchor" href="#1.-Offspring-draw" aria-label="Permalink to &quot;1. Offspring draw {#1.-Offspring-draw}&quot;">​</a></h4><p>Pure branching process, no time, no interventions.</p><p>For a parent of type <code>j</code>, contacts are drawn from an offspring distribution. This is the classical branching process: a count from a distribution (single-type) or a vector of counts per type (multi-type). The mean (R) and overdispersion (k) come from the offspring distribution. If a contact matrix is provided, the mixing pattern across types is determined by it.</p><h4 id="2.-Timing" tabindex="-1">2. Timing <a class="header-anchor" href="#2.-Timing" aria-label="Permalink to &quot;2. Timing {#2.-Timing}&quot;">​</a></h4><p>Generation times are assigned independently to each contact.</p><p>Each contact is given a generation time sampled from a distribution (either fixed or derived from the parent&#39;s incubation period, as in the ringbp-style model). This generation time is the <em>potential</em> time of transmission — the infectiousness profile, or h(t) in survival analysis terms.</p><h4 id="3.-Competing-risks" tabindex="-1">3. Competing risks <a class="header-anchor" href="#3.-Competing-risks" aria-label="Permalink to &quot;3. Competing risks {#3.-Competing-risks}&quot;">​</a></h4><p>Which contacts are actually infected?</p><p>Each contact is resolved independently:</p><ul><li><p>Was the parent isolated before this contact&#39;s generation time? (hazard truncation)</p></li><li><p>Is the contact susceptible? (vaccination, prior immunity, population depletion)</p></li><li><p>Is the parent still infectious? (infectiousness modifier)</p></li><li><p>For leaky isolation: does residual transmission succeed?</p></li></ul><p>Contacts that fail any of these checks are not infected but are still stored. They represent contacts that were made but did not result in transmission. Intervention effort (contacts traced, vaccines administered, tests used) is tracked this way.</p><h3 id="Why-this-separation-matters" tabindex="-1">Why this separation matters <a class="header-anchor" href="#Why-this-separation-matters" aria-label="Permalink to &quot;Why this separation matters {#Why-this-separation-matters}&quot;">​</a></h3><p>Because the offspring distribution is a pure probabilistic object, it can be analysed with standard tools: extinction probability from the dominant eigenvalue, chain size distributions, analytical likelihoods. None of this depends on timing or interventions.</p><p>At the intervention stage, the timing layer is modified, not the offspring layer. The hazard is truncated by isolation. The truncation point is shifted earlier by contact tracing. Susceptibility is modified by vaccination. All of these are competing risks on the same set of potential contacts.</p><p>The generation time CDF evaluated at the intervention time <em>is</em> the survival function. The same mathematical objects appear in simulation and in Kenah&#39;s pairwise likelihood framework. Intervention effectiveness can be estimated from observed generation times using the same quantities used in simulation.</p><p>All contacts are stored, not just successful infections. This gives the <strong>simulist</strong>-style contacts table (with <code>was_case</code> flag) and intervention effort tracking, without any additional bookkeeping.</p><h2 id="Individual-state" tabindex="-1">Individual state <a class="header-anchor" href="#Individual-state" aria-label="Permalink to &quot;Individual state {#Individual-state}&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Individual (struct)</span></span>
<span class="line"><span>├── Core (used by the engine)</span></span>
<span class="line"><span>│   ├── id, parent_id, generation, chain_id    # transmission tree</span></span>
<span class="line"><span>│   ├── infection_time                          # timing</span></span>
<span class="line"><span>│   ├── susceptibility, infectiousness          # universal modifiers</span></span>
<span class="line"><span>│   └── secondary_case_ids                      # filled during simulation</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>└── state::Dict{Symbol, Any}                    # everything else</span></span>
<span class="line"><span>    ├── :onset_time, :asymptomatic, :test_positive   # clinical (set by init)</span></span>
<span class="line"><span>    ├── :isolated, :isolation_time                     # set by Isolation</span></span>
<span class="line"><span>    ├── :traced, :quarantined                          # set by ContactTracing</span></span>
<span class="line"><span>    ├── :infected                                      # set by competing risk resolution</span></span>
<span class="line"><span>    └── :age, :sex, :risk_group, ...                   # user-defined</span></span></code></pre></div><p>Only <code>susceptibility</code>, <code>infectiousness</code>, and <code>infection_time</code> are read by the engine. Everything else is owned by interventions, attributes functions, or the user. Fields are initialised via <code>initialise_individual!</code> and accessed through accessor functions with safe defaults.</p><h2 id="Intervention-interface" tabindex="-1">Intervention interface <a class="header-anchor" href="#Intervention-interface" aria-label="Permalink to &quot;Intervention interface {#Intervention-interface}&quot;">​</a></h2><p>Three hooks, all optional:</p><ul><li><p><code>initialise_individual!(intervention, individual, state)</code> — set up fields on a new contact</p></li><li><p><code>resolve_individual!(intervention, individual, state)</code> — determine intervention state before transmission (e.g. compute isolation time from onset + delay)</p></li><li><p><code>apply_post_transmission!(intervention, state, new_contacts)</code> — act on contacts after creation (e.g. contact tracing, ring vaccination). All contacts, infected and non-infected, are passed.</p></li></ul><p>Interventions are stacked in a vector and applied in order. Each intervention has its own fields on the individual and declares what fields it requires.</p><h3 id="Time-based-scheduling" tabindex="-1">Time-based scheduling <a class="header-anchor" href="#Time-based-scheduling" aria-label="Permalink to &quot;Time-based scheduling {#Time-based-scheduling}&quot;">​</a></h3><p>Interventions with a <code>start_time</code> field represent policies that are not available from the start. The framework enforces <code>start_time</code> generically: after each hook call, it checks whether the intervention&#39;s effect on an individual falls before the policy start and undoes it if so.</p><p>Each intervention declares:</p><ul><li><p><code>intervention_time(intervention, individual)</code> — the time at which the effect occurs (e.g. isolation time, trace time). Default: <code>-Inf</code> (always applies).</p></li><li><p><code>reset!(intervention, individual)</code> — reverts all state changes made by the intervention. Default: no-op.</p></li><li><p><code>start_time(intervention)</code> — the policy start time. Default: <code>0.0</code>.</p></li></ul><p>The check is on <em>action time</em>, not infection time. An individual infected before the policy start can still be affected if their computed action time (e.g. <code>onset + delay</code>) falls after <code>start_time</code>. This is the correct competing-risk interpretation: the testing infrastructure must be available at the time the individual would be tested.</p><p>The <code>Scheduled</code> wrapper adds population-level conditions (case-count triggers, time windows, custom predicates) on top of this mechanism. It forwards <code>start_time</code> to the inner intervention when provided.</p><h2 id="Transmission-modifiers" tabindex="-1">Transmission modifiers <a class="header-anchor" href="#Transmission-modifiers" aria-label="Permalink to &quot;Transmission modifiers {#Transmission-modifiers}&quot;">​</a></h2><p>All interventions ultimately map onto two numbers:</p><ul><li><p><code>susceptibility</code> (0–1): probability of being infected given exposure. Reduced by vaccination, prior immunity, or population depletion.</p></li><li><p><code>infectiousness</code> (0–1): modifier on onward transmission. Reduced by isolation (via hazard truncation), treatment, or asymptomatic status.</p></li></ul><p>For a contact to be infected, it must survive the parent&#39;s infectiousness check AND the contact&#39;s susceptibility check AND the timing check (generation time vs isolation time).</p><h2 id="Multi-type-branching-processes" tabindex="-1">Multi-type branching processes <a class="header-anchor" href="#Multi-type-branching-processes" aria-label="Permalink to &quot;Multi-type branching processes {#Multi-type-branching-processes}&quot;">​</a></h2><p>Multiple types (age groups, risk groups, spatial patches) are supported in the offspring draw. For a parent of type <code>j</code>, offspring counts per type are drawn from a joint distribution. The mixing pattern comes from a contact matrix; the count distribution comes from the offspring distribution family.</p><p>Each contact is allocated to a type, stored as <code>:type</code> in its state dict. Interventions and output are unchanged — they operate on individual-level state, not types.</p><h2 id="Composing-analytical-methods" tabindex="-1">Composing analytical methods <a class="header-anchor" href="#Composing-analytical-methods" aria-label="Permalink to &quot;Composing analytical methods {#Composing-analytical-methods}&quot;">​</a></h2><p>The simulation side is extended by implementing the intervention protocol. The analytical side uses a different pattern. Each extension is a Julia type with a method on <code>chain_size_distribution</code> or <code>loglikelihood</code>, or both. There is no new abstract type beyond the existing <code>TransmissionModel</code>; multiple dispatch handles the rest.</p><h3 id="Two-kinds-of-extension" tabindex="-1">Two kinds of extension <a class="header-anchor" href="#Two-kinds-of-extension" aria-label="Permalink to &quot;Two kinds of extension {#Two-kinds-of-extension}&quot;">​</a></h3><p>Offspring specifications replace what a branching process draws per individual. An example is <code>ClusterMixed(build, mixing)</code>, which lets the offspring distribution&#39;s parameters vary from chain to chain. These are stored in <code>BranchingProcess.offspring</code> and participate via:</p><ul><li><p><code>_draw_offspring(rng, offspring_spec, individual, state)</code> for simulation</p></li><li><p><code>chain_size_distribution(offspring_spec)</code> for analytics (returns a distribution)</p></li></ul><p>Transmission-model wrappers modify how a model is observed. They subtype <code>TransmissionModel</code> and hold a wrapped model. <code>PartiallyObserved(model, detection_prob)</code> is the current example. They participate via:</p><ul><li><p><code>chain_size_distribution(wrapper)</code> returning a distribution that transforms the wrapped model&#39;s chain size distribution (e.g. <code>ThinnedChainSize(chain_size_distribution(m.model), p)</code>)</p></li><li><p><code>loglikelihood(data, wrapper)</code>, which usually just routes through <code>chain_size_distribution</code></p></li></ul><h3 id="Why-wrappers-return-distributions" tabindex="-1">Why wrappers return distributions <a class="header-anchor" href="#Why-wrappers-return-distributions" aria-label="Permalink to &quot;Why wrappers return distributions {#Why-wrappers-return-distributions}&quot;">​</a></h3><p><code>chain_size_distribution(m::PartiallyObserved)</code> returns <code>ThinnedChainSize(base, p)</code>. The result is itself a <code>DiscreteUnivariateDistribution</code> with its own <code>logpdf</code>, so a future wrapper such as <code>TimeCensored</code> can wrap it: <code>CensoredChainSize(ThinnedChainSize(base, p), p_c)</code>. Composition is then just nesting the distributions.</p><p>If instead the thinning were computed inside <code>loglikelihood(::PartiallyObserved)</code>, adding a second wrapper would require working out its joint likelihood with <code>PartiallyObserved</code> by hand, and every new pair would need the same treatment. Returning a distribution means each wrapper can be written once.</p><h3 id="Closed-forms-as-dispatch-optimisations" tabindex="-1">Closed forms as dispatch optimisations <a class="header-anchor" href="#Closed-forms-as-dispatch-optimisations" aria-label="Permalink to &quot;Closed forms as dispatch optimisations {#Closed-forms-as-dispatch-optimisations}&quot;">​</a></h3><p>Some combinations of offspring and mixing have closed forms. Poisson offspring with a Gamma-distributed rate gives <code>PoissonGammaChainSize</code>, which is the <code>gborel</code> likelihood from <code>epichains</code>. These plug in by specialising <code>chain_size_distribution</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chain_size_distribution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ClusterMixed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ChainSizeMixture</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">build, o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mixing)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chain_size_distribution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ClusterMixed{PoissonFamily, &lt;:Gamma}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PoissonGammaChainSize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The generic case uses adaptive Gauss-Kronrod quadrature via <code>ChainSizeMixture</code>. When a closed form applies, dispatch uses it without the user having to ask.</p><h3 id="Pipe-composition" tabindex="-1">Pipe composition <a class="header-anchor" href="#Pipe-composition" aria-label="Permalink to &quot;Pipe composition {#Pipe-composition}&quot;">​</a></h3><p>Each wrapper has a single-argument form that returns a function taking the model, so Julia&#39;s pipe works:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PartiallyObserved</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.7</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PartiallyObserved</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PartiallyObserved</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># compounds to 0.25</span></span></code></pre></div><p>When the wrappers don&#39;t commute, order matters and the pipe reads left to right.</p><h2 id="Simulation,-mutation,-and-automatic-differentiation" tabindex="-1">Simulation, mutation, and automatic differentiation <a class="header-anchor" href="#Simulation,-mutation,-and-automatic-differentiation" aria-label="Permalink to &quot;Simulation, mutation, and automatic differentiation {#Simulation,-mutation,-and-automatic-differentiation}&quot;">​</a></h2><p>The simulation engine uses in-place mutation: <code>step!</code> appends individuals, updates case counts, and modifies individual state via interventions. This is deliberate — branching process simulations grow an unbounded tree, and copying the full state at every generation would be prohibitively expensive.</p><h3 id="Analytical-likelihoods" tabindex="-1">Analytical likelihoods <a class="header-anchor" href="#Analytical-likelihoods" aria-label="Permalink to &quot;Analytical likelihoods {#Analytical-likelihoods}&quot;">​</a></h3><p><code>loglikelihood(ChainSizes(data), Poisson(R))</code> and similar analytical methods are deterministic scalar functions of the parameters. They work with any AD backend (ForwardDiff, Enzyme, Mooncake, ReverseDiff) and with gradient-based samplers like NUTS.</p><h3 id="Simulation-based-likelihoods" tabindex="-1">Simulation-based likelihoods <a class="header-anchor" href="#Simulation-based-likelihoods" aria-label="Permalink to &quot;Simulation-based likelihoods {#Simulation-based-likelihoods}&quot;">​</a></h3><p><code>loglikelihood(ChainSizes(data), model; interventions=...)</code> runs stochastic simulations internally. Because the simulation draws random numbers, the output is a noisy estimate of the true likelihood, not a smooth function of the parameters. Gradients of a single stochastic realisation are not useful estimates of the gradient of the expected likelihood. Gradient-based samplers (NUTS, HMC) should not be used here.</p><p>Use gradient-free samplers instead: Metropolis-Hastings (<code>MH()</code>), particle methods, or similar. The inference tutorial demonstrates this with <code>MH()</code>.</p><h3 id="Sim-analytical-consistency" tabindex="-1">Sim ↔ analytical consistency <a class="header-anchor" href="#Sim-analytical-consistency" aria-label="Permalink to &quot;Sim ↔ analytical consistency {#Sim-analytical-consistency}&quot;">​</a></h3><p>An extension with both an analytical chain size distribution and a simulation path should have a regression test confirming they agree. The test suite has a helper at <code>test/testutils/sim_analytical_consistency.jl</code>. A new type plugs in by defining two methods:</p><ul><li><p><code>generative_model(m)</code> strips observation wrappers so <code>simulate_batch</code> can run</p></li><li><p><code>observe_chain_sizes(m, true_sizes, rng)</code> transforms simulated true chain sizes into observed ones (defaults to the identity; observation wrappers override it)</p></li></ul><p><code>sim_analytical_consistent(model; n_chains, sizes, rng)</code> then simulates, applies <code>observe_chain_sizes</code>, and compares the empirical PMF against <code>chain_size_distribution(model)</code>. The same helper already covers bare offspring, <code>ClusterMixed</code>, and <code>PartiallyObserved</code>.</p><h2 id="Connection-to-survival-analysis" tabindex="-1">Connection to survival analysis <a class="header-anchor" href="#Connection-to-survival-analysis" aria-label="Permalink to &quot;Connection to survival analysis {#Connection-to-survival-analysis}&quot;">​</a></h2><p>The generation time distribution g(t) = h(t)/R is the normalised infectiousness profile. Its CDF, G(t), is the cumulative hazard. When isolation occurs at time t_iso, the probabilities are:</p><ul><li><p>P(transmission before isolation) = G(t_iso)</p></li><li><p>P(transmission after isolation) = 1 - G(t_iso)</p></li></ul><p>The result is right-censoring of the transmission process. The generation time distribution (derived from the hazard) is connected to the population growth rate through the Euler-Lotka equation R = 1/M_g(-r).</p><p>The same objects – the generation time distribution and the censoring time – appear in both simulation and Kenah&#39;s pairwise likelihood. Inference built on top of this framework would fit the same quantities we simulate from.</p><h2 id="References" tabindex="-1">References <a class="header-anchor" href="#References" aria-label="Permalink to &quot;References {#References}&quot;">​</a></h2><ul><li><p>Kenah E, Lipsitch M, Robins JM (2008). Generation interval contraction and epidemic data analysis. <em>Mathematical Biosciences</em> 213(1):71–79. <a href="https://doi.org/10.1016/j.mbs.2008.02.007" target="_blank" rel="noreferrer">doi:10.1016/j.mbs.2008.02.007</a></p></li><li><p>Kenah E (2011). Contact intervals, survival analysis of epidemic data, and estimation of R0. <em>Biostatistics</em> 12(3):548–566. <a href="https://doi.org/10.1093/biostatistics/kxq068" target="_blank" rel="noreferrer">doi:10.1093/biostatistics/kxq068</a></p></li><li><p>KhudaBukhsh WR, Choi B, Kenah E, Rempala GA (2020). Survival dynamical systems: individual-level survival analysis from population-level epidemic models. <em>Interface Focus</em> 10(1):20190048. <a href="https://doi.org/10.1098/rsfs.2019.0048" target="_blank" rel="noreferrer">doi:10.1098/rsfs.2019.0048</a></p></li><li><p>Wallinga J, Lipsitch M (2007). How generation intervals shape the relationship between growth rates and reproductive numbers. <em>Proceedings of the Royal Society B</em> 274(1609):599–604. <a href="https://doi.org/10.1098/rspb.2006.3754" target="_blank" rel="noreferrer">doi:10.1098/rspb.2006.3754</a></p></li></ul>`,77)])])}const m=e(n,[["render",o]]);export{u as __pageData,m as default};
